# 规则文档边界

Status: accepted

AGENTS.md（及套件内规则文档）只承载导航与长期约束，不是事实手册；端口、版本、业务码、路径、接口签名等具体值一律以代码现状为准，发现文档与代码矛盾当场修正本文件。选择「运行时自愈」而非「实时同步」：事实已决定不写入文档，机器无法核对「约束该怎么改」的语义判断；代价是 agent 须养成读文档时核对代码的习惯，长期约束的语义变化仍需人工更新。

背景：原则提炼自 sweet-agent-coding ADR-0001（CLAUDE.md 职责边界）。
